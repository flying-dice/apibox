{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://apibox.dev/schemas/user-profile.json",
  "$comment": "displayName intentionally allows any string -- do not add a format keyword here without checking with the identity team first.",
  "$vocabulary": {
    "https://json-schema.org/draft/2020-12/vocab/core": true,
    "https://json-schema.org/draft/2020-12/vocab/applicator": true,
    "https://json-schema.org/draft/2020-12/vocab/format-annotation": false
  },
  "title": "User Profile",
  "description": "A user's public profile.",
  "type": "object",
  "properties": {
    "id": { "type": "string", "format": "uuid" },
    "displayName": { "type": "string", "examples": ["Ada Lovelace"] },
    "address": { "$ref": "#/$defs/Address" }
  },
  "required": ["id", "displayName"],
  "$defs": {
    "Address": {
      "type": "object",
      "examples": [{ "street": "1 Bridge St", "city": "London" }],
      "properties": {
        "street": { "type": "string" },
        "city": { "type": "string" }
      }
    }
  }
}
